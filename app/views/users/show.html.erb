<%= render "shared/header" %>
<div class="my__page__main">マイページ</div>
<div class="my__post__area">
<% @post.each do |post| %>
<%= post.title %><br>
<%= image_tag post.image %><br>
<%= l post.created_at %><br>
<%=  post.content %>
<% end %>
</div>
    <%# if current_user.id == @user_id %>
      <%= link_to "#{current_user.nickname}情報変更",  user_registration_path, method: :patch, class: "item-red-btn" %>
      <%= link_to "Delete Account",  user_registration_path, method: :delete, class:"item-destroy", id: "delete-confirmation",onclick: "return deleteCheck()" %>
      <%#else%>
    <%# end %>
     <%= link_to "チャットページに戻る", :back, class: 'btn'%>
<%= render "shared/footer" %>