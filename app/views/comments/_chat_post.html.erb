<%= form_with model: [@post, @comment], class: 'form', id: "form", local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :chat, placeholder: 'tabiiiにメッセージを送ってみよう!!' %>
  </div>
  <%= f.submit '送信', id:"submit" %>
<% end %>
