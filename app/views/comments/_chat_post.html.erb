<%= form_with model: [@post, @comment], class: 'form', local: true do |f| %>
  <div class="form-input">
    <%= f.text_field :chat, placeholder: 'type a message' %>
  </div>
  <%= f.submit '送信' %>
<% end %>
